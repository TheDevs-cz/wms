{% extends 'base.html.twig' %}

{% block title %}Sklady{% endblock %}

{% block content %}
    <h1>Sklady</h1>

    <p>
        <a class="btn btn-outline-primary" href="{{ path('warehouse_add') }}">
            <i class="bi bi-plus-lg"></i> Přidat sklad
        </a>
    </p>

    <div class="table-responsive shadow-lg">
        <table class="table table-hover mb-0">
            <thead class="table-dark">
                <tr>
                    <th scope="col">Název</th>
                    <th scope="col">Stav</th>
                    <th scope="col" class="text-end">Akce</th>
                </tr>
            </thead>
            <tbody>
                {% for warehouse in warehouses %}
                    <tr>
                        <td>{{ warehouse.title }}</td>
                        <td>
                            {% if not warehouse.deactivatedAt %}
                                <span class="badge bg-success">Aktivní</span>
                            {% else %}
                                <span class="badge bg-warning">Neaktivní</span>
                            {% endif %}
                        </td>
                        <td class="text-end">
                            <a href="{{ path('warehouse_edit', {'id': warehouse.id}) }}" class="btn btn-sm btn-outline-primary me-1">
                                <i class="bi bi-pencil"></i> Editovat
                            </a>

                            {% if not warehouse.deactivatedAt %}
                                <a href="{{ path('warehouse_deactivate', {'id': warehouse.id}) }}" class="btn btn-sm btn-outline-danger">
                                    <i class="bi bi-slash-circle"></i> Deaktivovat
                                </a>
                            {% endif %}
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}
