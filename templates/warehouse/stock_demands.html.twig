{% extends 'base.html.twig' %}

{% block title %}Skladové nároky{% endblock %}

{% block content %}
    <h1>Skladové nároky</h1>

    <div class="table-responsive shadow-lg">
        <table class="table table-hover mb-0">
            <thead class="table-dark">
            <tr>
                <th scope="col">SKU</th>
                <th scope="col">Název</th>
                <th scope="col">EAN</th>
                <th scope="col">K vychystání</th>
                <th scope="col">Skladem</th>
                <th scope="col">Rozdíl</th>
            </tr>
            </thead>
            <tbody>
            {% for stock_demand in stock_demands %}
                <tr>
                    <td>{{ stock_demand.sku }}</td>
                    <td>{{ stock_demand.title }}</td>
                    <td>{{ stock_demand.ean }}</td>
                    <td>{{ stock_demand.unpickedOrderedQuantity }}</td>
                    <td>{{ stock_demand.stockQuantity }}</td>
                    <td>
                        {% if stock_demand.stockDifference < 0 %}
                            {{ stock_demand.stockDifference }}
                            <span class="badge text-bg-danger ms-1">
                                 Není skladem!
                            </span>
                        {% else %}
                            {{ stock_demand.stockDifference }}
                        {% endif %}
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
