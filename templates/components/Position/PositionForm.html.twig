<div {{ attributes }} class="card">
    <div class="card-body">
        {{ form_start(form, {
            attr: {
                'data-action': 'live#action:prevent',
                'data-live-action-param': position ? 'handleEdit' : 'handleAdd',
            }
        }) }}

        {{ form_widget(form) }}


        {{ form_end(form) }}
    </div>
    <div class="card-footer">
        <button type="submit" class="btn btn-primary">Uložit</button>
    </div>
</div>
