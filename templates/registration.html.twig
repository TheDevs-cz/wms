{% extends 'base.html.twig' %}

{% block title %}Registrace{% endblock %}

{% block full_content %}
    <div class="account-pages pt-2 pt-sm-5 pb-4 pb-sm-5 position-relative">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-xxl-4 col-lg-5">
                    <div class="card">

                        <div class="card-body p-4">
                            {{ include('_flashes.html.twig') }}

                            <div class="text-center w-75 m-auto">
                                <h4 class="text-dark-50 text-center mt-0 fw-bold">Registrace</h4>
                                <p class="text-muted mb-4">Registrace jsou nyní pouze na požádání. Pokud máte zájem o přístup do systému, napište nám na <a href="mailto:j.mikes@me.com">j.mikes@me.com</a>.</p>
                            </div>

                        </div>
                    </div>

                    <div class="row mt-3">
                        <div class="col-12 text-center">
                            <p class="text-muted">Zpět na <a href="{{ path('login') }}" class="text-muted ms-1"><b>přihlášení</b></a></p>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>
{% endblock %}
