{% extends 'base.html.twig' %}

{% block title %}Pozice{% endblock %}

{% block content %}
    <h1>Pozice</h1>

    <p>
        <a class="btn btn-outline-primary" href="{{ path('position_add') }}">
            <i class="bi bi-plus-lg"></i> Přidat pozici
        </a>

        <a class="btn btn-outline-primary" href="{{ path('position_generate') }}">
            <i class="bi bi-arrow-repeat"></i> Generovat pozice
        </a>
    </p>

    <div class="table-responsive shadow-lg">
        <table class="table table-hover mb-0">
            <thead class="table-dark">
                <tr>
                    <th scope="col">Název</th>
                    <th scope="col">Lokace</th>
                    <th scope="col">Sklad</th>
                    <th scope="col">Stav</th>
                    <th scope="col" class="text-end">Akce</th>
                </tr>
            </thead>
            <tbody>
                {% for position in positions %}
                    <tr>
                        <td>
                            <a href="{{ path('position_info', {'id': position.id}) }}">
                                {{ position.name }}
                            </a>
                        </td>
                        <td>
                            <a href="{{ path('location_info', {'id': position.location.id}) }}">
                                {{ position.location.name }}
                            </a>
                        </td>
                        <td>
                            <a href="{{ path('warehouse_info', {'id': position.location.warehouse.id}) }}">
                                {{ position.location.warehouse.title }}
                            </a>
                        </td>
                        <td>
                            {% if not position.deactivatedAt %}
                                <span class="badge bg-success">Aktivní</span>
                            {% else %}
                                <span class="badge bg-warning">Neaktivní</span>
                            {% endif %}
                        </td>
                        <td class="text-end">
                            <a target="_blank" href="{{ path('qr_code_generate', {'builder': 'default', 'data': url('position_info', {'id': position.id})}) }}" class="btn btn-sm btn-outline-primary me-1">
                                <i class="bi bi-printer"></i> Tisk štítku
                            </a>

                            <a target="_blank" href="{{ path('position_qr', {'id': position.id}) }}" class="btn btn-sm btn-outline-primary me-1">
                                <i class="bi bi-printer"></i> Tisk štítku 90x25
                            </a>

                            <a href="{{ path('position_edit', {'id': position.id}) }}" class="btn btn-sm btn-outline-primary me-1">
                                <i class="bi bi-pencil"></i> Editovat
                            </a>

                            {% if not position.deactivatedAt %}
                                <a href="{{ path('position_deactivate', {'id': position.id}) }}" class="btn btn-sm btn-outline-danger">
                                    <i class="bi bi-slash-circle"></i> Deaktivovat
                                </a>
                            {% endif %}
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}
